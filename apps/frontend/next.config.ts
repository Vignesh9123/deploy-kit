import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  allowedDevOrigins:['https://fe.vignesh-tech.com'] // TODO: Will remove once deployment is changed from dev to prod
};

export default nextConfig;
